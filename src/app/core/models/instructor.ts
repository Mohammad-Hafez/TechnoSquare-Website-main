export interface Instructor{
        id?: number,
        name?: string ,
        email?: string,
        phone?: string,
        image?:string ,
        date_of_birth?: string,
        city?: string,
        address?: string,
        gender?: string,
        date_of_join?:string ,
        salary?: number,
        job_type?: string,
        work_hours?: number,
        status?:number 
}